import { Input } from "@/components/ui/Input";

export function SsoOidStub() {
  return (
    <Input
      label="SSO / OIDC provider"
      placeholder="Not configured (stub)"
      disabled
    />
  );
}
