# Python dependencies for the social-footprint module's wrappers.
# Install into the Python environment the Go module will invoke:
#   pip install -r requirements.txt
#
# maigret: MIT-licensed. Embedded as a library by wrapper/maigret_check.py.
# sherlock-project: MIT-licensed. Embedded as a library by wrapper/sherlock_check.py.
# Both pinned to the exact version the wrappers and output parsing were
# verified against (see docs/decisions/stage-1-decision.md).
maigret==0.6.2
sherlock-project==0.16.1
