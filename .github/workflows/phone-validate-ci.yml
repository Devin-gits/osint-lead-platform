name: phone-validate CI
on:
  push:
    paths:
      - 'modules/phone-validate/**'
      - '.github/workflows/phone-validate-ci.yml'
  pull_request:
    paths:
      - 'modules/phone-validate/**'
      - '.github/workflows/phone-validate-ci.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22.5'
      - run: cd modules/phone-validate && go test ./...
      - run: cd modules/phone-validate && go build ./...
